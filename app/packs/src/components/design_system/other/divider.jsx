import React from 'react';

const Divider = ({
    title,
    shortCaption,
    placeholder,
    mode,
    disabled,
}) => {

    return (
        <>
            <div className="divider"></div>
        </>
    )
};

export default Divider;