<!DOCTYPE html>
<html>
  <head>
    <title>Talent Protocol</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_pack_tag "application", media: 'all' %>
  </head>

  <body>
    <div class="h-100 d-flex flex-column lg-overflow-hidden align-items-center">
      <div class="lg-overflow-hidden d-flex flex-row flex-wrap flex-lg-nowrap flex-lg-grow-1 flex-shrink-1 justify-content-center w-100">
        <%= render "shared/navbar" %>
        <%= render "shared/flashes" %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
