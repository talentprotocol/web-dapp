<!DOCTYPE html>
<html>
  <head>
    <title>Talent Protocol</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_pack_tag "application", media: 'all' %>
  </head>

  <body>

    <div class="d-flex flex-row flex-wrap align-items-stretch justify-content-center mx-auto" style="min-height: 1024px; max-width:1450px">
      <header class="col-12 col-lg-3 px-0 tal-sidebar">
        <%= render "shared/navbar" %>
      </header>
      <main class="col-12 col-lg-9 mx-auto mx-lg-0 px-0" style="max-width: 1200px">
        <%= render "shared/flashes" %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
