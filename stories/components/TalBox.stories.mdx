import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import TalBox from 'src/components/navbar/TalBox';

<Meta title="Components/TalBox" component={TalBox} />

# TalBox

Custom component to display current $TAL info on the sidebar.

## Example

export const Template = (args) => <TalBox {...args} />

<Canvas>
  <Story name="default" args={{ 
      price: "$1.65",
      variance: "3%"
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
