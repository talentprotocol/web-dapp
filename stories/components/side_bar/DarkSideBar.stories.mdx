import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import SideBar from 'src/components/design_system/side_bar';

<Meta title="Design System/SideBar/Dark Mode" component={SideBar} />


export const Template = (args) => <SideBar {...args} />

<Canvas>
  <Story
    name="Side Bar"
    args={{
        mode: "dark",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
