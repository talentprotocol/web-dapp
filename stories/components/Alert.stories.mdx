import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Alert from 'src/components/alert';

<Meta title="Design System/Alert" component={Alert} />

# Alerts

Custom alerts to call out users to important information.

## Examples

We support any type that bootstrap supports. We simply add some custom styling.

export const Template = (args) => <Alert {...args} />

<Canvas>
  <Story name="Primary" args={{
      type: "primary",
      text: "Connect your wallet to buy Carrer Tokens",
      href: "https://metamask.io/",
      buttonText: "Connect MetaMask"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Info" args={{
      type: "info",
      text: "INFO ALERT",
      href: "/messages",
      buttonText: "Go to Messages!"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Success" args={{
      type: "success",
      text: "SUCCESS ALERT"
    }}>
    {Template.bind({})}
   </Story>
   <Story name="Warning" args={{
      type: "warning",
      text: "WARNING ALERT"
    }}>
    {Template.bind({})}
   </Story>
   <Story name="Danger" args={{
      type: "danger",
      text: "DANGER ALERT"
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
