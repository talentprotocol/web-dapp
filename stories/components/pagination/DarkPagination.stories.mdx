import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Pagination from 'src/components/design_system/pagination';

<Meta title="Design System/Pagination/Dark Mode" component={Pagination} />

export const Template = (args) => <Pagination {...args} />

<Canvas>
  <Story
    name="Pagination"  parameters={{backgrounds: { default: 'dark' }}}
    args={{
      mode: "dark",
      currentPage: 3,
      maxPages: 6,
      onChangePage: () => null,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>