import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import P1 from 'src/components/design_system/p1';

<Meta title="Components/P1" component={P1} />

# P1s

Custom P1s to show titles.

## Examples

export const Template = (args) => <P1 {...args} />

<Canvas>
  <Story
    name="Light mode"
    args={{
      mode: "light",
      text: "Example of a paragraph",
    }}>
    {Template.bind({})}
   </Story>
  <Story
    name="Light mode with bold"
    args={{
      mode: "light",
      bold: true,
      text: "Example of a paragraph with bold",
    }}>
    {Template.bind({})}
   </Story>
  <Story
    name="Dark mode"
    parameters={{backgrounds: { default: 'dark' }}}
    args={{
      mode: "dark",
      text: "Example of a paragraph"
    }}>
    {Template.bind({})}
   </Story>
   <Story
    name="Dark mode with bold"
    parameters={{backgrounds: { default: 'dark' }}}
    args={{
      mode: "dark",
      bold: true,
      text: "Example of a paragraph with bold"
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
