import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Button from 'src/components/button';

<Meta title="Components/Button" component={Button} />

# Buttons

Custom button styles for actions in forms, dialogs, and more with support for multiple sizes, states, and more.

## Examples

There are 6 currently supported types, `primary`, `info`, `success`, `warning`, `danger` and `secondary`. We use react-bootstrap button underneath so we accept any prop they use.

export const Template = (args) => <Button {...args} />

<Canvas>
  <Story name="Primary" args={{ 
      text: "Primary!",
      onClick: () => null,
      type: "primary"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Secondary" args={{ 
      text: "Secondary!",
      onClick: () => null,
      type: "secondary"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Danger" args={{ 
      text: "Danger!",
      onClick: () => null,
      type: "danger"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Warning" args={{ 
      text: "Warning!",
      onClick: () => null,
      type: "warning"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Info" args={{ 
      text: "Info!",
      onClick: () => null,
      type: "info"
    }}>
    {Template.bind({})}
   </Story>
  <Story name="Success" args={{ 
      text: "Success!",
      onClick: () => null,
      type: "success"
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
