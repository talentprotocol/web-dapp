import { Meta, Canvas, Story } from "@storybook/addon-docs";

import TextInput from "src/components/design_system/fields/textinput";

<Meta title="Design System/Fields/TextInput/Dark Mode" component={TextInput} />

export const Template = (args) => <TextInput {...args} />;

<Canvas>
  <Story
    name="Title"
    parameters={{ backgrounds: { default: "dark" } }}
    args={{
      title: "Title here",
      placeholder: "Placeholder goes here",
      mode: "dark",
      disabled: false,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="No title"
    parameters={{ backgrounds: { default: "dark" } }}
    args={{
      placeholder: "Placeholder goes here",
      mode: "dark",
      disabled: false,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="With short caption"
    parameters={{ backgrounds: { default: "dark" } }}
    args={{
      shortCaption: "Short caption",
      placeholder: "Placeholder goes here",
      mode: "dark",
      disabled: false,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Disabled"
    parameters={{ backgrounds: { default: "dark" } }}
    args={{
      placeholder: "Placeholder goes here",
      mode: "dark",
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
