
import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Tooltip from 'src/components/design_system/tooltip';

<Meta title="Design System/Tooltip" component={Tooltip} />

# Tooltips

Custom Tooltips.

## Examples

export const Template = (args) => <Tooltip {...args} />

<Canvas>
  <Story
    name="Light mode with title and body"
    args={{
      title: "Tooltip title",
      body: "Tooltip body",
      children: <h1 style={{textAlign: "center"}}>Hover here</h1>,
      popOverAccessibilityId: "1",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Light mode only with body"
    args={{
      body: "Tooltip body",
      children: <h1 style={{textAlign: "center"}}>Hover here</h1>,
      popOverAccessibilityId: "2",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Dark mode with title and body"
    parameters={{backgrounds: { default: 'dark' }}}
    args={{
      title: "Tooltip title",
      body: "Tooltip body",
      children: <h1 style={{textAlign: "center"}}>Hover here</h1>,
      popOverAccessibilityId: "3",
      mode: 'dark',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Dark mode only with body"
    parameters={{backgrounds: { default: 'dark' }}}
    args={{
      body: "Tooltip body",
      children: <h1 style={{textAlign: "center"}}>Hover here</h1>,
      popOverAccessibilityId: "4",
      mode: 'dark',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>