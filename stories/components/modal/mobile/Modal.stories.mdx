import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Modal from 'src/components/design_system/modal/mobile';

<Meta title="Design System/Mobile/Modal/Modes" component={Modal} />

# Modal

Multiple type of modals

export const Template = (args) => <Modal {...args} />

<Canvas>
  <Story
    name="Simple modal"
    args={{
      mode: "light",
      title: "Modal title",
      description: "Description goes here"
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Modal with button"
    args={{
      mode: "light",
      title: "Modal title",
      description: "Description goes here",
      button_text: "Click here",
       onClick: () => null,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Simple modal dark"
    args={{
      mode: "dark",
      title: "Modal title",
      description: "Description goes here"
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Modal with button dark"
    args={{
      mode: "dark",
      title: "Modal title",
      description: "Description goes here",
      button_text: "Click here",
       onClick: () => null,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>