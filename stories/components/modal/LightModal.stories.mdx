import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import Modal from 'src/components/design_system/modal/staking';
import ModalContact from 'src/components/design_system/modal/contact';

<Meta title="Design System/Modal/Light Mode" component={Modal} />

# Modal

Multiple type of modals

export const Template = (args) => <Modal {...args} />
export const TemplateContact = (args) => <ModalContact {...args} />

<Canvas>
  <Story
    name="Staking modal"
    args={{
      mode: "light",
      amount: "4 230.80",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Contact modal"
    args={{
      mode: "light",
    }}
  >
    {TemplateContact.bind({})}
  </Story>
</Canvas>